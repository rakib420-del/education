// JWT strategy removed — auth now uses email OTP + activeSessionToken session guard.
// This file is intentionally empty. The SessionGuard in auth/guards/session.guard.ts
// handles all user authentication.
export {};
